@model IEnumerable<ShopOnlineModel.EF.tb_Menus>

@{
    if (Model.Count() > 0)
    {
        <div class="footer-menu">
            <div class="footer-menu-header">
                Về chúng tôi
            </div>
            <div class="footer-menu-body">
                <ul class="nav navbar-nav">
                    @foreach (var item in Model)
                    {
                        <li>
                            <a href="@item.Url" title="@item.Text">
                                @item.Text
                            </a>
                        </li>
                    }
                </ul>
            </div>
        </div>

    }
}

